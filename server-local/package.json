{
  "name": "server-app",
  "version": "1.0.0",
  "description": "mock json server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-server": "json-server --watch db.json",
    "start-server-alt": "json-server --watch db.json --port 3004"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
